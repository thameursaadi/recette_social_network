I"L(function() {
  this.cooking.controller('listRecipeCtrl', function($scope, $http) {
    var req;
    req = $http.get("http://localhost:3000/recipe/recipelist.json");
    $scope.recipes = [];
    return req.success(function(data) {
      $scope.recipes = data;
      $.each($scope.recipes, function(index, recipe) {
        var s;
        return s = 0.0;
      });
      return $.each(recipe.rates, function(i, d) {
        var s;
        console.log(d.value);
        return s = parseFloat(s) + parseFloat(d.value);
      });
    });
  });

  console.log($scope.recipes);

}).call(this);
:ET